<?xml version="1.0" encoding="utf-8"?>
<network-security-config>

    <!-- Base config: allow all HTTP (safe for local dev) -->
    <base-config cleartextTrafficPermitted="true" />

</network-security-config>
